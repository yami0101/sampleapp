<%= form_for(current_user.likes.find_by(micropost_id: @micropost),
             html: {method: :delete}, remote: true) do |f| %>
    <%= f.submit "#{@micropost.likes.count} likes.", class: "btn btn-success"%>
<% end %>