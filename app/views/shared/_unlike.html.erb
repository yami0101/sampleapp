<%= form_for(current_user.likes.find_by(micropost_id: @micropost.id),
             remote: true, html: {method: :delete}) do |f| %>
    <%= f.hidden_field :micropost_id %>
    <%= f.submit "#{@micropost.likes.count} likes.", class: "btn btn-success"%>
<% end %>