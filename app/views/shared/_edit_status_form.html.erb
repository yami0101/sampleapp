<% @user = current_user %>
<%= form_for(@user, html: {class: "status-form"}, remote: true,
             url: {action: 'update_status'}) do |f| %>
  <%= f.text_field(:status, maxlength: 50) %>
  <%= f.submit('') %>
<% end %>