$currentLikes = $('.microposts li[data-id="<%= @micropost.id %>"] .likes');
$currentLikes
  .html("<%= escape_javascript(render('shared/like')) %>");

$('a[href="<%= liked_user_path(current_user) %>"] #likes').text("<%= current_user.likes.count %>");